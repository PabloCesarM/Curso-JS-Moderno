// diferencia entrefunciones y métodos
const numero1 = 20;
const numero2 = '20';

// esto es una funcion, la funcion va antes de la variable
console.log(parseInt(numero2));

// convertir a string
console.log(numero2.toString()); 
//si primero tiene la variable y lo sigue un . para llamar, es un metodo

